[Unit]
Description=Tailscale Serve - HTTPS Proxy
Documentation=https://tailscale.com/kb/1242/tailscale-serve
After=network.target tailscaled.service opencode.service deutsch-app.service
Requires=tailscaled.service
Wants=opencode.service deutsch-app.service

[Service]
Type=oneshot
RemainAfterExit=yes

# Wait for Tailscale to be fully connected (prevent "netMap is nil" errors)
# Polls every 2 seconds for up to 120 seconds
ExecStartPre=/bin/bash -c 'for i in {1..60}; do /usr/bin/tailscale status --json 2>/dev/null | grep -q '"'"'"BackendState": "Running"'"'"' && exit 0; sleep 2; done; echo "Tailscale not ready after 120s"; exit 1'

ExecStart=/bin/bash -c '\
  /usr/bin/tailscale serve reset && \
  /usr/bin/tailscale serve --bg --https=443 http://localhost:{{ opencode_port }} && \
  /usr/bin/tailscale serve --bg --https=443 --set-path /deutsch http://localhost:{{ deutsch_app_port }}/deutsch'
ExecStop=/usr/bin/tailscale serve reset

# Restart policy: retry on failure (e.g. tailscale not ready on boot)
Restart=on-failure
RestartSec=30s
StartLimitInterval=300s
StartLimitBurst=5

[Install]
WantedBy=multi-user.target
